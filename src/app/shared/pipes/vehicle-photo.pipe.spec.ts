import { VehiclePhotoPipe } from './vehicle-photo.pipe';

describe('VehiclePhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiclePhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
